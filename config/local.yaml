env: "local" # local,dev,prod
http_server:
  addres: "localhost:8080"
  timeout: 4s
  idle_timeout: 60s
  user: "myuser"
  password: "mypass"
storage:
  type: "MongoDB"
  SQLite:
    path: "./storage.db"
  
  MongoDB: 
    uri: "mongodb://@localhost:27017/url-shortener"
